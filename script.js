let items = ["Rock", "Paper", "Scissors"];

let computerPlay = () => items[Math.floor(Math.random() * items.length)];

let round = (playerSelection, computerSelection) =>{
    let playerSelection = p
    let computerSelection = c

    if (p === "Rock" && c === ) {
        return "You Win! " + p + " beats" + v
    }
}
